name: feature_splash
description: Splash feature — session check and navigation decision

version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^9.1.1
  injectable: ^2.6.0
  get_it: ^9.1.1
  commons_navigation:
    path: ../../commons/navigation
  commons_security:
    path: ../../commons/security
  commons_user:
    path: ../../commons/user

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.0
  injectable_generator: ^2.12.1
  mocktail: ^1.0.4
  fake_async: ^1.3.2
