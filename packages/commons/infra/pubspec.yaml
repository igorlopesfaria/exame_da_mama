name: commons_infra
description: Infrastructure module — HTTP client, exceptions and DI setup

version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.8.0+1
  flutter_secure_storage: ^10.0.0
  fpdart: ^1.1.0
  get_it: ^9.1.1
  injectable: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.11.0
  injectable_generator: ^2.12.1
  mocktail: ^1.0.4
